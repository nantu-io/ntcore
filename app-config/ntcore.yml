##############################
## Local configurations
##############################
container:
  provider:
    type: docker

database:
  provider:
    type: sqlite
    path: data/db/ntcore.db

storage:
  provider:
    type: volume
    config:
      root: data/binary