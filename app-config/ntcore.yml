################################
## Configs for local deployment
################################
container:
  provider:
    type: docker
  images:
    sklearn: ntcore/fast-sklearn
    tensorflow: tensorflow/serving
    pytorch: ntcore/fast-torch
    bootstrap: ntcore/bootstrap
    metricsProxy: ntcore/metrics-proxy

database:
  provider:
    type: sqlite
    config:
      path: data/db/ntcore.db

storage:
  provider:
    type: volume
    config:
      root: data/binary

## Default userId when user auth is disabled
account: 
  username: ntcore

#####################################
## Example config for AWS deployment
#####################################
# container:
#   provider:
#     type: kubernetes
#     config:
#       namespace: default
#   images:
#     sklearn: ntcore/fast-sklearn
#     tensorflow: ntcore/tensorflow-serving
#     pytorch: ntcore/fast-torch

# database:
#   provider:
#     type: dynamodb
#     config:
#       region:
#       accessKeyId:
#       secretAccessKey:

# storage:
#   provider:
#     type: s3
#     config:
#       bucket: ntcore-model-storage
#       root: binary
#       region:
#       accessKeyId:
#       secretAccessKey:

# account: 
#   username: ntcore